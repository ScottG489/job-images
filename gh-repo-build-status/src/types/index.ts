export interface RepoBadgeInfo {
  repoName: string;
  repoUrl: string;
  badgeUrl: string;
}

export interface RepoBuildInfo {
  repoName: string;
  repoUrl: string;
  badgeUrl: string;
  workflowRunConclusion: string;
}
